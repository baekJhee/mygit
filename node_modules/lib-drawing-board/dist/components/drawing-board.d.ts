import { LitElement, PropertyValues } from 'lit';
import { DrawingTool } from '../types';
type ReplayPhase = 'start' | 'move' | 'end';
export interface ReplayEventDetail {
    phase: ReplayPhase;
    x?: number;
    y?: number;
    tool?: DrawingTool;
    color?: string;
    width?: number;
}
export declare class DrawingBoard extends LitElement {
    private svgElement;
    private ctrl;
    width: number;
    height: number;
    get tool(): DrawingTool;
    set tool(val: DrawingTool);
    get color(): string;
    set color(val: string);
    get lineWidth(): number;
    set lineWidth(val: number);
    showGuides: boolean;
    private fitMode;
    private resizeObserver;
    private cursorInfo;
    private _onReplayStart;
    private _onReplayMove;
    private _onReplayEnd;
    static styles: import('lit').CSSResult;
    connectedCallback(): void;
    disconnectedCallback(): void;
    protected updated(changedProps: PropertyValues): void;
    /**
     * 초기 width/height가 설정되지 않았다면 부모 크기로 설정
     */
    private initSize;
    private checkFit;
    undo(): void;
    redo(): void;
    canUndo(): boolean;
    canRedo(): boolean;
    clear(): void;
    setSplitMode(enabled: boolean): void;
    exportData(): import('..').DrawingData[];
    exportAsBlob(type?: 'svg' | 'png'): Promise<Blob>;
    importData(json: string, side?: 'left' | 'right' | 'full'): void;
    applyReplayEvent(detail: ReplayEventDetail): void;
    private _applyReplayStyle;
    private _handlePointerDown;
    private _handlePointerMove;
    private _stopAndDispatch;
    private renderStroke;
    private renderEraserCursor;
    render(): import('lit-html').TemplateResult<1>;
}
export {};
