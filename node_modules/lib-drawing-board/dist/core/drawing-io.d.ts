import { BoardPosition, DrawingData, Stroke } from '../types';
export declare class DrawingIO {
    /**
     * 데이터를 내보내기 위한 구조로 변환
     */
    static createExportData(strokes: Stroke[], width: number, height: number, isSplitMode: boolean): DrawingData[];
    /**
     * JSON 문자열을 파싱하고, 현재 캔버스에 맞게 병합된 새로운 Stroke 배열을 반환
     */
    static parseAndMerge(jsonString: string, currentStrokes: Stroke[], width: number, height: number, position?: BoardPosition, shouldHeal?: boolean): Stroke[];
    /**
     * 선을 경계선에서 정확히 절단
     */
    private static cutStrokeExact;
    private static checkStrokePosition;
    private static shiftStroke;
    private static scaleStroke;
    private static isStrokeRightSide;
}
