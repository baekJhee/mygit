import { Point } from '../types';
/**
 * 점 배열(Point[])을 부드러운 SVG Path 문자열('M x y Q ...')로 변환
 * 알고리즘: Quadratic Bezier Curves using Midpoints
 */
export declare function getSvgPathFromStroke(points: Point[]): string;
