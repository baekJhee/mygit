import { ReactiveController, ReactiveControllerHost } from 'lit';
import { BoardPosition, DrawingData, DrawingTool, Point, Stroke } from '../types';
export interface DrawingBoardHost extends ReactiveControllerHost {
    width: number;
    height: number;
    requestUpdate(): void;
}
/**
 * 드로잉 상태 및 설정 관리 컨트롤러
 * 비지니스 로직 분리
 */
export declare class DrawingController implements ReactiveController {
    host: DrawingBoardHost;
    strokes: Stroke[];
    currentPoints: Point[];
    isDrawing: boolean;
    private history;
    private historyStep;
    private configs;
    private _tool;
    private _color;
    private _lineWidth;
    private _hasEraserChange;
    private isSplitMode;
    constructor(host: DrawingBoardHost);
    hostConnected(): void;
    hostDisconnected(): void;
    private _trySetPointerCapture;
    private _tryReleasePointerCapture;
    get tool(): DrawingTool;
    /**
     * 도구 변경 로직
     * @returns 변경된 설정값 객체 (뷰에서 이벤트 발송용)
     */
    setTool(val: DrawingTool): {
        color: string;
        width: number;
    } | null;
    get color(): string;
    setColor(val: string): void;
    get lineWidth(): number;
    setLineWidth(val: number): void;
    get isSplitModeState(): boolean;
    /**
     * 현재 그리는 중인 선(Draft)을 위한 설정값 반환
     */
    getCurrentStrokeConfig(): {
        tool: DrawingTool;
        color: string;
        width: number;
        opacity: number;
        lineCap: string;
    };
    /**
     * 현재 상태를 히스토리에 저장 (Action이 끝났을 때 호출)
     */
    private saveState;
    get canUndo(): boolean;
    get canRedo(): boolean;
    undo(): void;
    redo(): void;
    /**
     * 캔버스 완전 초기화 (히스토리까지 삭제)
     */
    clear(): void;
    setSplitMode(enabled: boolean): void;
    exportData(): DrawingData[];
    exportAsBlob(svgElement: SVGSVGElement, type?: 'svg' | 'png'): Promise<Blob>;
    importData(jsonString: string, position?: BoardPosition): void;
    start(e: PointerEvent, svgElement: SVGSVGElement): void;
    startAt(x: number, y: number): void;
    private _drawMove;
    move(e: PointerEvent, svgElement: SVGSVGElement): void;
    moveTo(x: number, y: number): void;
    erase(x: number, y: number): void;
    stop(e: PointerEvent): void;
    stopReplay(): void;
    private _finalizeStop;
}
